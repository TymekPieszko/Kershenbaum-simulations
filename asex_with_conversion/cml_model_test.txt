DIR=/data/zool-barralab/scro4331/simon
MUT=5.0e-09
L=1e7
SEX_FREQ=0.0
TRACT_LEN=5000
N=1000
GENS=5000
REP=0

GC_RATE=1.0e-09
D=0.1


mkdir -p ${DIR}/output/fitness/${GC_RATE}/${D}/
mkdir -p ${DIR}/output/vcf/${GC_RATE}/${D}/
time slim -d GC_RATE=$GC_RATE -d D=$D -d MUT=$MUT -d L=$L -d SEX_FREQ=$SEX_FREQ -d TRACT_LEN=$TRACT_LEN -d N=$N -d GENS=$GENS -d "FIT_FILE='${DIR}/output/fitness/${GC_RATE}/${D}/${REP}.txt'" -d "VCF_FILE='${DIR}/output/vcf/${GC_RATE}/${D}/${REP}.vcf'" ${DIR}/asex_with_conversion_muts.slim